# Embedded Scripts

The name is due to the fact that these scripts are normally embedded at the end
of the tmux.conf. But if so desired, they can also be generated and referred to
as external scripts
